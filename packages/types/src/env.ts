// Env types are defined in @workkit/env which owns the Standard Schema-based
// env validation system. This module is intentionally empty — it was removed
// to eliminate duplication between @workkit/types and @workkit/env.
