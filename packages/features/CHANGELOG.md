# @workkit/features

## 0.1.0

### Minor Changes

- New package: KV-backed feature flags with percentage rollouts, audience
  targeting, A/B testing variants, and user overrides. Includes deterministic
  hashing (FNV-1a) for sticky assignment, in-memory TTL cache, admin CRUD,
  and Hono middleware.
